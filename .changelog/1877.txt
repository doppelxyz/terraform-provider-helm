```release-note:bug
`resource/helm_release`: Fix `set_sensitive` values never actually being redacted from the `manifest` and `resources` attributes when `experiments.manifest` is enabled, including values that contain JSON-special characters (quotes, backslashes, newlines).
```
